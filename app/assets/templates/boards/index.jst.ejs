<ul>
  <% boards.each(function(board) { %>
    <li><a href="#/<%= board.id %>">
      <%= board.escape("title") %>
      <button class="btn-delete-board" data-id="<%= board.id %>">
        Delete
      </button>
    </a></li>
  <% }); %>
    <li class="new-board-form">
      <button class="btn-create-board">Create</button>
    </li>
</ul>

<button class=".btn-refresh-board">Refresh</button>

